{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist"
  },
  "include": ["src/**/*.ts"],
  "//": "No `references`. The business logic layer sits at the bottom of the import graph and is not permitted to see any other workspace."
}
